{% extends "base/layout.twig" %}

{% block content %}
<h1>Passwort zurücksetzen</h1>

{% if error %}
<p class="error">{{ error }}</p>
{% endif %}

<form method="post" action="{{ url_for('pw_forgot_post') }}">
  <p>
    <label>E-mail: <input type="text" name="email" value="{{email}}" required></label>
  </p>
  <p>
    <button type="submit">Reset-Link schicken</button>
  </p>
</form>
<p><a href="{{ url_for('login') }}">Zurück zum Login</a></p>
{% endblock %}
