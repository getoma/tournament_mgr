<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>{% block title %}Tournament powered by Slim{% endblock %}</title>
  <link rel="stylesheet" href="{{base_path}}/css/tree.css">
</head>
<body>
  {% if tournament is defined -%}
  <header>
    <h1>{{tournament.name}}</h1>
    <nav>{% if return_to is defined %}<a href="{{base_path}}{{return_to}}">&lt;&lt;Zurück</a>{%endif%}
      <a href="{{base_path}}/tournament/{{tournament.id}}">Hauptseite</a>
    </nav>
  </header>
  {%- endif %}
  <main>
    {% block content %}{% endblock %}
  </main>
</body>

</html>