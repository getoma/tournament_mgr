<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>{% block title %}Tournament powered by Slim{% endblock %}</title>
  <!-- Lokales CSS -->
  <link rel="stylesheet" href="{{ url_for('home') }}css/style.css">
  <link rel="stylesheet" href="{{url_for('home')}}css/tree.css">
  {% block special_links %}{% endblock -%}
</head>
<body>

  <!-- Header -->
  <header class="header">
  <div class="header-container">
    <h1>
      {%- block heading -%}
        {{ tournament.name|default('Kendo Turniersoftware') }}
      {%- endblock -%}
    </h1>

    <nav class="nav-actions">
      {% if current_user -%}
    	<div class="user-menu">
		<button class="user-menu-btn">
        	👤 {{ current_user.display_name }} ▼
    </button>

      		<div class="user-menu-dropdown">
			<a href="{{url_for('user_account')}}">Account</a>
        		<a href="{{url_for('logout')}}">Logout</a>
      		</div>
    	</div>
      {%-else%}
        <a href="{{url_for('login')}}" class="btn-login">Login</a>
      {%-endif%}
    </nav>
  </div>
  </header>

  <!-- Main Content -->

<main class="main-content">

      {% if current_user -%}
  <aside>
      <nav class="tournament">
  <ul>
    {% if return_to is not empty %}
      <li><a href="{{ url_for(return_to, route_args) }}">&laquo; Zurück</a></li>
    {% endif %}

    <li><a href="{{ url_for('home') }}">Turniere Übersicht</a></li>

    {% if tournament is defined %}
      <li><a href="{{ url_for('show_tournament', {'tournamentId': tournament.id}) }}">Turnierkategorien</a></li>
    {% endif %}

    <li><a href="{{ url_for('list_users') }}">Benutzerverwaltung</a></li>

    {% block nav_extension %}{% endblock %}
  </ul>
</nav>
    </aside>
 {%-endif%}

    <section id="content">
      {% block content %}{% endblock %}
    </section>
  </main>

</body>
</html>
